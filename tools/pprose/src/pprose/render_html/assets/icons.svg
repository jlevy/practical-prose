<svg xmlns="http://www.w3.org/2000/svg" style="display:none">
  <!-- Minimal placeholder symbol set. Group icons are rendered inline
       in the card via the template's `group.icon` field (Unicode glyphs),
       so this file is reserved for future expansion. -->
  <symbol id="pp-dot" viewBox="0 0 16 16"><circle cx="8" cy="8" r="3" fill="currentColor"/></symbol>
</svg>
