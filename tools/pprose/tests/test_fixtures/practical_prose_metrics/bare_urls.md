# Document with Bare URLs

See https://example.com for details on this topic.

Another reference at https://docs.example.com/api/v2 in the text.

This is an inline link [not bare](https://inline.example.com) which should not count as
bare.

And an autolink <https://auto.example.com> which is also not bare.
